name: peluqueria_aja
description: "Aplicación de reservas para peluquería"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Estado
  provider: ^6.1.1
  
  # HTTP y Red
  dio: ^5.4.0
  http: ^1.1.2
  
  # Persistencia
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0   # ✅ AÑADIDO AQUÍ (SIN QUITAR NADA)
  
  # Imágenes
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  
  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Si tienes imágenes en assets/, descomenta la siguiente línea
  # assets:
  #   - assets/
